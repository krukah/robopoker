[package]
name           = "rbp-nlhe"
version        = "0.1.0"
edition        = "2024"
rust-version   = "1.90"
authors        = ["Kelechi Ukah <ukahkelechi@gmail.com>"]
description    = "No-Limit Hold'em specific CFR implementation"
license        = "MIT"

[features]
default        = []
database       = ["tokio-postgres", "const_format", "async-trait", "rbp-database"]
server         = ["rbp-mccfr/server"]

[dependencies]
rbp-core       = { path = "../util" }
rbp-cards      = { path = "../cards" }
rbp-gameplay   = { path = "../gameplay" }
rbp-transport  = { path = "../transport" }
rbp-mccfr      = { path = "../mccfr" }
rbp-dto        = { path = "../dto" }
rbp-database   = { path = "../database", optional = true }
log            = "0.4"
rand           = { version = "0.9.1", features = ["small_rng"] }
anyhow         = "1.0"
tokio-postgres = { version = "0.7", optional = true }
const_format   = { version = "0.2", optional = true }
async-trait    = { version = "0.1", optional = true }
