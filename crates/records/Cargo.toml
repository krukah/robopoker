[package]
name         = "rbp-records"
version      = "1.0.0"
edition      = "2024"
rust-version = "1.90"
authors      = ["Kelechi Ukah <ukahkelechi@gmail.com>"]
description  = "Poker hand history recording and replay"
license      = "MIT"
repository   = "https://github.com/krukah/robopoker"
keywords     = ["poker", "hand-history", "records", "replay"]
categories   = ["game-development", "database"]

[features]
default      = []
database     = ["tokio-postgres", "const_format", "rbp-database"]

[dependencies]
rbp-core       = { version = "1.0", path = "../util"                     }
rbp-cards      = { version = "1.0", path = "../cards"                    }
rbp-gameplay   = { version = "1.0", path = "../gameplay"                 }
rbp-auth       = { version = "1.0", path = "../auth"                     }
rbp-database   = { version = "1.0", path = "../database", optional = true }
tokio-postgres = { version = "0.7",    optional = true      }
const_format   = { version = "0.2",    optional = true      }
